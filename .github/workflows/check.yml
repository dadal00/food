name: Check

on:
  push:
    branches:
      - "**"

jobs:
  backend:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@stable

      - name: Configure cache
        uses: Swatinem/rust-cache@v2

      - name: Compiling, formatting, and testing
        run: |
          cargo fmt --all -- --check
          cargo check
          cargo test
